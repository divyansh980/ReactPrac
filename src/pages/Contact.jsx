/**
 * The Contact component is a functional React component that renders a simple Contact Page with a
 * heading.
 * @returns The Contact component is being returned, which is a functional component that renders a div
 * containing an h1 element with the text "Contact Page".
 */
import React from 'react'

const Contact = () => {
  return (
    <div>
      <h1>Contact Page</h1>
    </div>
  )
}

export default Contact
