/**
 * The above function is a React component named Product that renders a simple product page with a
 * heading.
 * @returns The `Product` component is being returned, which is a simple functional component in React
 * that renders a `div` containing an `h1` element with the text "Product Page".
 */
import React from 'react'

const Product = () => {
  return (
    <div>
      <h1>Product Page</h1>
    </div>
  )
}

export default Product
